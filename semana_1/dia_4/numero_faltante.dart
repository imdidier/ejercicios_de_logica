void main(List<String> args) {
  List<int> numbers = [1, 3, 4, 5, 6, 7, 8];
  List<int> aux = [];
  // numbers.fold<List<int>>(aux, (previousValue, element) {
  //   int index = previousValue.
  //   if (previousValue[]!= element) {}
  //   return aux;
  // });
}
